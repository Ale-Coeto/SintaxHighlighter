import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)


import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)

import re

# Función para leer las categorías léxicas desde un archivo
def read_lexical_categories(file):
    categories = {}
    with open(file, 'r') as f:
        for line in f:
            if '=' in line:
                key, value = line.split('=', 1)
                categories[key.strip()] = value.strip().strip('"')
    return categories

# Función para resaltar una categoría léxica
def sum2(num1, num2):
    return num1 + num2

# Función para resaltar el texto según las categorías léxicas
def print_array(array):
    for item in array:
        print(item)

# Crear un array de prueba
array = ["hola", "mundo", 124, "456", "789"]

# Llamada de prueba
print_array(array)